/**
 * @author Jaimee Bennett
 * Student ID: 5048095
 * Project: Breakout
 * Date:30/1/22
 * Update:12/02/22
 * Version: 5.0
 * Aim: building ball object framework
 */

import java.awt.Graphics;


public class Ball extends Sprite {

	private int xVelocity = 1, yVelocity = -1;


	// Constructor
	public Ball() {
		
		// Set width for ball
		setWidth(Settings.BALL_WIDTH);
		// Set height for ball
		setHeight(Settings.BALL_HEIGHT);
		// Call resetPosition
		resetPosition();
	}
	
	/**
	 * Resets the ball to the initial position
	 */
	public void resetPosition() {
		
		// Set the balls x
		x = (Settings.INITIAL_BALL_X);
		// Set the balls y
		y = (Settings.INITIAL_BALL_Y); 
	}
	
	public void update() {
		// Increase the x variable by xVelocity
		x += xVelocity; 
		// Increase the y variable by yVelocity
		y += yVelocity; 
		
		// Bounce off left side of screen
		if(x <= 0) { 
			
			// Set x to 0 so it does not leave the screen
			x = 0;
			// Change the x velocity to make the ball go right
			setXVelocity(1);
			
		}
		
		// Bounce off right side of screen
		if(x >= Settings.WINDOW_WIDTH - (Settings.BALL_WIDTH + 10)) { /*added extra 10 as it was still going outside the frame a little but still bouncing off the side*/
			// Setting x to the right edge of the screen
			// Changing the x velocity to make the ball go left
			setXVelocity(-1);
		}
		
		// Bounce off top of screen
		if(y <= 0) {
			// Setting y to 0 so it does not leave the screen
			y = 0;
			// Changing the y velocity to make the ball go downward
			setYVelocity(1);
		}
				
	}
	
	public void setXVelocity(int x) {
		// sets the x velocity
		xVelocity = x; 
	}
	public void setYVelocity(int y) {
		// sets the y velocity
		yVelocity = y; 
	}
	
	public int getXVelocity() {
		// returns the x velocity
		return x;	
	}
	public int getYVelocity() {
		// returns the y velocity
		return y;	
	}
	
	public void paint(Graphics g) {
		g.setColor(Settings.BALL_COLOUR);
		g.fillOval(x, y, Settings.BALL_WIDTH, Settings.BALL_HEIGHT);
	}
}

